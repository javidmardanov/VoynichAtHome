<script lang="ts">let {data}=$props();</script>
<svelte:head><title>{data.campaign.title} — Voynich@home</title></svelte:head>
<div class="page narrow"><p class="eyebrow">Campaign · {data.campaign.status}</p><h1>{data.campaign.title}</h1><p class="lede">{data.campaign.question}</p><span class="badge">Scientific status: {data.campaign.scientific_status}</span>
<section class="panel"><h2>Declared scope</h2><p>{data.manifest.interpretation}</p><dl><dt>Metric</dt><dd>{data.manifest.metric}</dd><dt>Stopping condition</dt><dd>{data.manifest.stopping_rule}</dd><dt>Previous exposure</dt><dd>{data.manifest.exposure}</dd></dl><a href={data.manifest.protocol_url}>Full protocol ↗</a></section>
<h2>Reproduction records</h2><p>Records include the exact work specification and trusted replay output. An operational error is separate from a scientific outcome.</p><a class="button secondary" href={'/api/v1/campaigns/'+data.campaign.id}>Download records (JSON) →</a><p class="small muted" style="margin-top:15px">The interface returns up to 50 records per page. Follow its next cursor for the complete campaign. Each work identity links to its full input and checked result.</p><div class="table-wrap"><table><thead><tr><th>Work identity</th><th>Execution state</th></tr></thead><tbody>{#each data.records as record}<tr><td><a href={'/api/v1/records/'+encodeURIComponent(record.id)}><code>{record.id}</code></a></td><td>{record.state}</td></tr>{/each}</tbody></table></div>
<details><summary>Versioned campaign manifest</summary><p class="small"><code>{data.campaign.manifest_digest}</code></p><pre>{JSON.stringify(data.manifest,null,2)}</pre></details></div>
